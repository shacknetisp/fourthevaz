# -*- coding: utf-8 -*-
#profiles definition
profiles = {
  'freenode': {
    'whitelist': [
      (99, ["behalebabo"]),
      ],
    #set noauth to true if the network doesn't support logging in
    #if noauth is true, the adminlist uses nick rather than login
    'noauth': False,
    'adminlist': [
      (1000, "behalebabo"),
      ],
    'names': [
      (["behalebabo"], "Beha"),
      ]}
    }

